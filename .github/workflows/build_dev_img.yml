name: Build Dev Aliyun Image

on:
  push:
    branches: [ "githubaction" ]
    tags:
      - "v*.*.*"
  # pull_request:
  #   branches: [ "main" ]


jobs:
  build:
    name: Build and Push
    runs-on: [self-hosted, ailab]
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Docker meta
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: registry.cn-hangzhou.aliyuncs.com/aigitlab/actiondev
        tags: |
          type=raw,value=latest


    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to Aliyun Container Registry
      uses: docker/login-action@v2
      with:
        registry: registry.cn-hangzhou.aliyuncs.com
        username: ${{ secrets.Ali_USERNAME }}
        password: ${{ secrets.Ali_PASSWORD }}


    - name: Build and push
      uses: docker/build-push-action@v4
      with:
        context: .
        platforms: linux/amd64
        # platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

  sync-git:
    name: Sync to Git
    runs-on: [self-hosted, ailab]
    needs:
      - build
    steps:
      - name: Sync to Gitlab
        uses: wearerequired/git-mirror-action@master
        env:
          # 注意在 Settings->Secrets 配置 GITEE_RSA_PRIVATE_KEY
          SSH_PRIVATE_KEY: ${{ secrets.GITLAB_RSA_PRIVATE_KEY }}
        with:
          # 注意替换为你的 GitHub 源仓库地址
          source-repo: git@github.com:aiworklab/actiondev.git
          # 注意替换为你的 Git 目标仓库地址
          destination-repo: ${{ secrets.GITLAB_REPO }}

